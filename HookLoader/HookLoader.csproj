<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <OutputType>WinExe</OutputType>
        <TargetFrameworks>net35;net40</TargetFrameworks>
        <ImplicitUsings>disable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <LangVersion>13</LangVersion>
        <ApplicationIcon>titanic.ico</ApplicationIcon>
        <Version>0.1.1</Version>
        <Company>Titanic!</Company>
    </PropertyGroup>
    
    <PropertyGroup Condition="'$(TargetFramework)' == 'net35'">
        <AssemblyName>Titanic!net35</AssemblyName>
        <AssemblyDescription>Titanic! Loader (.NET Framework 3.5)</AssemblyDescription>
    </PropertyGroup>

    <PropertyGroup Condition="'$(TargetFramework)' == 'net40'">
        <AssemblyName>Titanic!net40</AssemblyName>
        <AssemblyDescription>Titanic! Loader (.NET Framework 4)</AssemblyDescription>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
      <PlatformTarget>x86</PlatformTarget>
      <Prefer32bit>true</Prefer32bit>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
      <PlatformTarget>x86</PlatformTarget>
      <Prefer32bit>true</Prefer32bit>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\TitanicHookManaged\TitanicHookManaged.csproj" />
    </ItemGroup>

    <ItemGroup>
      <Reference Include="System.Windows.Forms" />
    </ItemGroup>

    <ItemGroup>
      <PackageReference Include="ILMerge" Version="3.0.41" />
    </ItemGroup>
    
    <!-- ILMerge job for .NET Framework 3.5 build -->
    <Target Name="ILMerge-net35" AfterTargets="Build" Condition="'$(Configuration)' == 'Release' And '$(TargetFramework)' == 'net35'">
        <Exec Command="&quot;$(PkgILMerge)\tools\net452\ILMerge.exe&quot; /targetplatform:v2,&quot;C:\Program Files (x86)\Reference Assemblies\Microsoft\Framework\.NETFramework\v3.5\Profile\Client&quot; /out:&quot;$(OutputPath)$(AssemblyName)_merged.exe&quot; &quot;$(OutputPath)$(AssemblyName).exe&quot; &quot;$(OutputPath)0Harmony.dll&quot; &quot;$(OutputPath)ILReader.dll&quot; &quot;$(OutputPath)TitanicHookManaged.dll&quot;"/>
    </Target>

    <!-- ILMerge job for .NET Framework 4 build -->
    <Target Name="ILMerge-net40" AfterTargets="Build" Condition="'$(Configuration)' == 'Release' And '$(TargetFramework)' == 'net40'">
        <Exec Command="&quot;$(PkgILMerge)\tools\net452\ILMerge.exe&quot; /targetplatform:v4,&quot;C:\Program Files (x86)\Reference Assemblies\Microsoft\Framework\.NETFramework\v4.0&quot; /out:&quot;$(OutputPath)$(AssemblyName)_merged.exe&quot; &quot;$(OutputPath)$(AssemblyName).exe&quot; &quot;$(OutputPath)0Harmony.dll&quot; &quot;$(OutputPath)ILReader.dll&quot; &quot;$(OutputPath)TitanicHookManaged.dll&quot;"/>
    </Target>
</Project>
