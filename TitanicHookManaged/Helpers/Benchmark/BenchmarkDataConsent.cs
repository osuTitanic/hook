namespace TitanicHookManaged.Helpers.Benchmark;

/// <summary>
/// Consent from user to allow submitting benchmark scores
/// </summary>
public enum BenchmarkDataConsent
{
    NotAsked = -1,
    NotAllowed = 0,
    Allowed = 1,
}
